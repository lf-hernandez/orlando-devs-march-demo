[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[build.buildArgs]
COMMIT = "$RAILWAY_GIT_COMMIT_SHA"
VERSION = "$RAILWAY_GIT_BRANCH"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
