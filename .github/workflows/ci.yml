name: ci

on:
  push:
    branches: [main, develop, staging, production, 'release/**', 'hotfix/**', 'feature/**']
  pull_request:
    branches: [main, develop, staging, production]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    concurrency:
      group: ci-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: 'go.mod'
          cache: true
      - name: Verify dependencies
        run: go mod verify
      - name: Run go vet
        run: go vet ./...
      - name: Run tests
        run: go test -race -count=1 -timeout=120s ./...
